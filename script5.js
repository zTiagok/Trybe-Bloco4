const aNumber = 60;
const bNumber = 24;
const cNumber = 23;

if (aNumber < 0 || bNumber < 0 || cNumber < 0)
{
    
    console.log("These degrees CAN NOT be used to make a triangle.");
}
else if (aNumber > 60 || bNumber > 60 || cNumber > 60)
{

    console.log("These degrees CAN NOT be used to make a triangle.")  ;  
}
else
{

    console.log("These degrees can be used to make a triangle.");
}


// FEITO!